[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gnumake", "gcc", "pkg-config"]

[phases.install]
cmds = ["npm ci --ignore-scripts", "npm rebuild better-sqlite3"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server/index.js"
